import styled from 'styled-components'
import { Form } from 'antd'

export const CreateAccessForm = styled(Form)`
  display: flex;
  flex-direction: column;
  row-gap: 14px;
  padding-top: 20px;
`

export const CreateAccessFormFooter = styled.div`
  display: flex;
  justify-content: flex-end;
  column-gap: 10px;
  width: 100%;
  margin-top: 10px;
`
