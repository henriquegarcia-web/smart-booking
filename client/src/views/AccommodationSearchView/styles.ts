import styled from 'styled-components'
import { Sizes } from '@/utils/styles/globals'

export const AccommodationSearchView = styled.div`
  display: flex;
  flex-direction: column;
  row-gap: ${Sizes.spacing};
  width: 100%;
`
